name: glove
version: 0.0.1

authors:
  - Denis Defreyne <denis@stoneship.org>

license: MIT

dependencies:
  spec2:
    github: waterlink/spec2.cr
    branch: master

scripts:
  postinstall: cd src/ext && make
